# R NHSD 1 Set-up directories


########################################## Set-up ##########################################

# Specify where the data is stored
data_dir <- "A:/WHERE/THE/DATA/ARE/"

# HES, NJR, PROMS, ONS raw data should be in separate folders named as such within data_dir
# Ensure there are also empty folders called - HES_new, SQL, TEMP, R_IMAGES, FIGS

# Specify where the scripts are stored
script_dir <- "A:/WHERE/THE/SCRIPTS/ARE/"

# Note
# '/' is needed on the end of a path
# And, if copying a path from File Explorer, note that '\' need to be converted to '/'